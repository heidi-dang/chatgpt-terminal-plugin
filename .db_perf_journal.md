# DB Performance Journal

**Environment:** local cptr workspace (chatgpt-terminal-plugin), branch `feat/durable-sqlite-and-redis-live-ha`  
**Safety:** no production env file; SQLite embedded + optional Redis URL only.

## Prior accepted work (pre-journal)

| Iter | Domain | Hypothesis | Outcome | Action |
|------|--------|------------|---------|--------|
| P1 | C | MGET for list prune | N GET → 1 MGET | Committed |
| P2 | C | Multi-member SREM | N SREM → 1 | Committed |
| P3 | C | Skip pre-GET on Lua put | −1 RTT write | Committed |
| P4 | C | SADD/EXPIRE inside Lua | put/presence → 1 EVAL | Committed |
| P5 | C | SQLite composite indexes | no TEMP B-TREE sort | Committed |
| P6 | B | deleteSession field peek | ~247ms parse → ~0.2ms extract | Committed `0072d78` |
| P7 | A | Document maxmemory policy | ops note only | Committed |

## Loop log

| Iter | Domain | Hypothesis | Outcome | Action |
|------|--------|------------|---------|--------|
| 1 | C | `deleteSession` single Lua EVAL (GET+SREM+DEL) | ≤3 RTTs → 1; tests 11/11 | Committed `ce6af3d` |
| 2 | A | SQLite connection pool | Single-writer embedded SQLite; pool adds lock overhead, no ≥2% gain | Discarded (no code change) |
| 3 | C | Redis cache in front of device registry | Stale enroll/revoke risk without invalidation design | Discarded (no code change) |
| 4 | B | Split session event buffers into Redis HASH/list | Breaking schema + API | Discarded — diminishing returns |

**Plateau:** 3 consecutive non-commits after Iter 1 across A/C/B. Loop stopped.

