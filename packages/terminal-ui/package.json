{
  "name": "@terminal/terminal-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && node ../../scripts/check-terminal-ui-bundle.mjs",
    "dev": "vite --host 127.0.0.1",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "vite": "^7.1.4",
    "vite-plugin-singlefile": "^2.3.0"
  }
}
